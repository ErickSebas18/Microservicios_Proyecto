# ================================================================
# Nombre de la aplicación
# ================================================================
spring.application.name=proyecto_integrador

# ================================================================
# Configuración de la base de datos (PostgreSQL)
# ================================================================
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://dpg-d0k0173e5dus73bb81vg-a.oregon-postgres.render.com/proyecto_integrador_x35u
spring.datasource.username=proyecto_integrador_user
spring.datasource.password=azfBTTPHnFPr1Ux6J802y1YXO6uHwwn8

# ================================================================
# Configuración de JPA e Hibernate
# ================================================================
spring.jpa.database=POSTGRESQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# ================================================================
# Configuración del servidor
# ================================================================
server.port=9091

# ================================================================
# Configuración de seguridad (Keycloak / OAuth2)
# ================================================================
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/spring-boot-realm
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8080/realms/spring-boot-realm/protocol/openid-connect/certs

# Conversor de JWT (atributo principal y recurso permitido)
jwt.auth.converter.atributo-principal=preferred_username
jwt.auth.converter.recurso-id=spring-client-api-rest

# Credenciales del cliente OAuth2 (si aplica)
client-id=spring-client-api-rest
client-secret=4giXEm1UoTSQFGMF6UNiLJTmoISd9xjz

# ================================================================
# Configuración de Consul
# ================================================================
consul.host=localhost
consul.port=8500

# ================================================================
# Configuración de clientes Feign
# ================================================================
feign.client.usuario-service.url=http://localhost:7070/app-usuarios/usuarios
feign.client.documento-service.url=http://localhost:7070/app-documentos

# ================================================================
# Configuración CORS (permitir orígenes del frontend)
# ================================================================
cors.allowed-origins=http://localhost:3000
