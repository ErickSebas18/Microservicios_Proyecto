apiVersion: apps/v1
kind: Deployment
metadata:
  name: keycloak
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keycloak
  template:
    metadata:
      labels:
        app: keycloak
    spec:
      containers:
        - name: keycloak
          image: proyectoint.azurecr.io/keycloak-custom:latest
          imagePullPolicy: Always
          args:
            - "start-dev"
          env:
            - name: KEYCLOAK_ADMIN
              value: "admin"
            - name: KEYCLOAK_ADMIN_PASSWORD
              value: "admin"
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: keycloak-storage
              mountPath: /opt/keycloak/data
      volumes:
        - name: keycloak-storage
          emptyDir: {}

---

apiVersion: v1
kind: Service
metadata:
  name: keycloak
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-resource-group: ProyectoIntegrador
spec:
  type: LoadBalancer
  loadBalancerIP: 52.225.81.116
  selector:
    app: keycloak
  ports:
    - port: 8080
      targetPort: 8080
